# input() 함수
# 사용자로부터 정보를 입력 받는 함수
# user_input = input()
# print(user_input)

# 정수형 변환 함수
# int()
# integer형, 정수형, int형


# 실수형 float형
# 부동(浮動) 소수점
# 1, 2, 3 - 정수형
# 과학적 표기법 = 소수점 앞에 숫자 하나만 오게 하는 표기법.

# 10진수 데이터를 2진수로 바꿀 때 오차가 생기는 경우가 있음
# 단위 계산을 하는 경우 정수로 하는 것이 좋음.

# 실수형 변환 함수
# float()

# e = exponent = 지수

# list에서 사용하는 함수


# 리스트(list) 자료형
# 여러개의 값을 변수 1개에 저장
# 숫자와 문자를 같이 사용할 수 있음
# 인덱싱은 정수를 추출
# 슬라이싱은 리스트 자체를 추출, 즉 원형을 유지한 상태로 추출.

# 리스트 함수

#1 append(원소)
## 리스트의 마지막에 원소를 추가

# 컴퓨터에서의 행, 렬 표현법
# [[1, 2, 3],
# [4, 5, 6],
# [7, 8, 9]]
